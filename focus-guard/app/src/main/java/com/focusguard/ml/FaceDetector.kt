package com.focusguard.ml

import android.graphics.Bitmap

// Person 1: Face detection using BlazeFace TFLite model
class FaceDetector {
    fun detect(frameBitmap: Bitmap): FaceDetectionResult {
        // Person 1 replaces this with BlazeFace LiteRT inference.
        return FaceDetectionResult.NoFace
    }
}
